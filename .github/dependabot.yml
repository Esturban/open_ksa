# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for more information:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates
# https://containers.dev/guide/dependabot
#
# FIX 2026-09-04 (OPS-2955): the devcontainers ecosystem entry was removed.
# It failed every weekly run since 2026-07-20 with "Neither .devcontainer.json
# nor .devcontainer/devcontainer.json ... found in /" even though
# .devcontainer/devcontainer.json exists on this branch -- a Dependabot-side
# resolution mismatch this repo's files cannot fix. No other ecosystem is
# configured here; pip updates for this repo run through the separate
# "Dependency Graph" workflow, not dependabot.yml.

version: 2
updates: []
